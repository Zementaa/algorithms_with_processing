Alle Bilder können reproduziert werden, in dem die Konfiguration über die Schnelllade-Funktion MenuBar>>Beispiele>>BWinzen in den Konfigurator geladen werden. 
Über den Button "Testlauf" läuft dann die Animation, die an ihrem Ende das Bild erstellt hat.

Ausnahme:
kooperation_ImageEvoColorInduction.jpg & kooperation_jäger&gejagte.jpg sind Momentaufnahmen. Ihre Konfiguration ist so eingestellt, dass die Animation nie abbricht.
